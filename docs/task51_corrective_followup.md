# المتابعة التصحيحية المستقلة بعد المهمة 5.1

التاريخ: 16 سبتمبر 2026. الحالة: مكتملة ومغلقة ضمن نطاقها، ولم تبدأ 5.2 أو 5.3.

## نقطة البداية والنطاق

بدأت الجولة من HEAD `cbd2a17192971d136cd817a4132482c36d84c766` على `master`. كانت الشجرة تحتوي فقط على ملف المستخدم غير المتتبع `.task-4.1-request.txt`، وبقي بلا تعديل أو إضافة. أثبت `STATUS.md` أن 5.1 مغلقة وأن 5.2 و5.3 لم تبدآ.

## التشخيص والإصلاح

1. القوالب المختلطة: كانت قوالب `CommandContent` تمر إلى المترجم ديناميكيًا من غير وسم ثابت يلتقطه مستخرج gettext، لذلك لم تدخل رسائل F11 ومواقيت اليوم وحالة التنبيهات كتالوج العربية. وسمت المعرفات بـ`N_()`، وأضيفت ترجمات كاملة للقالب والحالات والأسماء، مع إبقاء Domain محايدًا والإنجليزية نص المصدر.
2. تأخير الضغطات: كان `MultiPressDispatcher` الخاص ينتظر 450 مللي ثانية قبل تنفيذ أول وظيفة. حُذف الملف، واستبدل بمسار مركزي متزامن يقرأ `scriptHandler.getLastScriptRepeatCount()` مع feature detection؛ تنفذ الضغطة الأولى داخل الاستدعاء نفسه ولا يوجد Timer حسم خاص.
3. خريطة الأوامر: كانت أوامر داخلية مستقلة تظهر مكررة، ولم تكن Ctrl+H وShift+P ضمن الخريطة النهائية. أصبحت 21 وظيفة ظاهرة بأوصاف بشرية مترجمة: 12 لها gestures افتراضية و9 بلا gesture، وأصبحت الوظائف الداخلية helpers غير ظاهرة.
4. 12 ساعة: وحّد تحديد الفترة في مساعد واحد يستقبل الساعة المحلية الأصلية قبل `% 12`، وتستخدمه العربية والإنجليزية وصيغتا الأرقام والكلمات. تغطي الاختبارات 00:00 و00:30 و06:00 و11:59 و12:00 و12:30 و18:00 و23:59.
5. معلومات اليوم: كان المنسق يبدأ بعناوين عامة ولا يحمل النموذج رقم اليوم داخل الفصل، كما لم يفصل سياق التقويم العربي المستقل عن المجمّع. أضيف `season_day` إلى القراءة والخدمة على أساس تاريخ الحدث المحلي، وأضيفت منسقات علمية مستقلة وعربية مستقلة ومجمعة، وحذفت مقدمة نوع سنة سهيل وطولها وباقي أيامها من النطق اليومي.
6. البند 7 — مدد حالة المواقيت: أضيف منسق مشترك واحد يحول 60 دقيقة فأكثر إلى ساعات ودقائق، ويحذف باقي الدقائق إذا كان صفرًا، مع صيغ العربية للمفرد والمثنى والجمع والحالة بعد «قبل»، ومقابلات إنجليزية طبيعية. بقي حساب الفرق الزمني في CommandContent نفسه بلا تغيير، واستعمل المنسق في القادم والسابق والانتظار والصلاة الحالية.

## خريطة الاختصارات النهائية

الافتراضية الاثنا عشر: `NVDA+Alt+A`، `NVDA+F11`، `NVDA+Shift+F11`، `NVDA+Ctrl+Shift+F11`، `NVDA+F12`، `NVDA+Shift+F12`، `NVDA+Alt+F12`، `NVDA+G`، `NVDA+H`، `NVDA+Shift+H`، `NVDA+Ctrl+H`، `NVDA+Shift+P`.

`Ctrl+H` يعلن العلمي فقط في الضغطة الأولى، والعربي فقط في الثانية، ويفتح نافذة معلومات اليوم في الثالثة وحدها. `Shift+P` يعرض في الأولى placeholder صادقًا بأن التحقق الشبكي ينتظر 5.3، ويفتح نافذة المواقيت في الثانية. لا توجد خدمة شبكة أو طلب شبكة في هذه الجولة.

الأوامر التسعة بلا اختصار افتراضي: التاريخ الميلادي، تبديل التقويم الأساسي، التوقيت الزوالي، تنبيهات الأذكار، تنبيهات المواقيت، تنبيه الساعة، ساعات الهدوء، التشخيص، وتحديث البيانات.

## بيانات التقويم العربي

فُحص ملف المالك `G:\ملفاتي\إضافة أوقاتي\التقويم العربي\arabian_calendar.json`: UTF-8 بلا BOM، JSON صالح، الحجم 71,049 بايت، `schemaVersion=1`، والتحقق بواسطة `arabian_calendar.schema.json` ناجح. أثبت الإسقاط البنيوي تطابق IDs وorder والتواريخ والأطوال والعلاقات والمراجع مع المصدر السابق؛ الفروق نصية فقط. لم تعد توليد فهارس common/leap، ورفع إصدار البيانات إلى `2026.09.16-r2` مع تحديث رؤوس الفهرسين والـmanifest والـmetadata والبصمات.

بصمة الملف المعتمد في المصدر وruntime واحدة: `de783557d1dc1fa7f094a957f4a33fb1d602b5f7c7e9b226233f4a4b67baf7dd`. يحتوي نص المالك حرفيًا على قيم مثل `" null."` داخل بعض الحقول النصية؛ احتُفظ بها بلا إعادة صياغة أو تحويل صامت امتثالًا لقرار عدم تحسين نص المالك آليًا.

## الاختبارات والقبول

نجحت المجموعة التصحيحية السابقة: 85/85، ونجحت اختبارات البند 7 الحدودية والتكاملية الستة ضمن البوابة. نجحت `tools/run_checks.py`: 736/736، صفر failures وerrors وskipped، مع compile والتنظيف والبناء وفحص الحزمة والمعمارية والترجمة و`git diff --check`. غطت القيم 0، 1، 2، 3، 10، 11، 26، 59، 60، 61، 62، 119، 120، 121، 122، 136، 180، 181، 182 و190 بالعربية والإنجليزية.

جرت جولتا قبول داخل NVDA 2026.2 الحقيقي بالعربية والإنجليزية بواسطة Scratchpad مؤقت مفوض به، بعد تعذر أداة التحكم الحاسوبي مرتين بخطأ ACL قبل أي إدخال. نجحت كل التحققات المسجلة: 21 أمرًا، 9 بلا اختصار، الأوصاف واللغة، F11 وF12 وG وH وShift+H وCtrl+H وShift+P بكل ضغطاتها، Alt+A، Shift+F11، Ctrl+Shift+F11، Shift+F12 وAlt+F12، وفصل المخرجات والنوافذ وسرعة أول ضغطة. أعادت اختبارات التبديل ملف إعدادات أوقاتي إلى بصمته نفسها.

أعيد قبول البند 7 داخل NVDA 2026.2 فعليًا بملف إعداد اختباري مستقل، بالعربية والإنجليزية: 26 دقيقة، و86 دقيقة، و136 دقيقة. طابقت العربية «26 دقيقة»، و«ساعة و26 دقيقة»، و«ساعتين و16 دقيقة» في سياقات القادم والسابق، وطابقت الإنجليزية `26 minutes` و`1 hour 26 minutes` و`2 hours 16 minutes`. استعيد `nvda.ini` ببصمة `352fd980344762c38c70b6b8d8ea9693fdd5bc213b923d59a6687265067e8458`، واستعيدت إعدادات أوقاتي ببصمة `3f784f011e1ed99cd5b9d2a6223ced68ab893a4ee3d211c3a2355d2523168d5d`. أزيلت أداة Scratchpad وملفها المخبأ، وأعيد تشغيل NVDA المعتاد، وبقي البناء 0.5.1.4 مثبتًا. لا يدعى اختبار جهاز برايل مادي.

تفاصيل القبول في `docs/task51_corrective_nvda_acceptance.json`.

## الوثائق والبناء والقيود

حدثت الخطتان النظرية والتنفيذية بقسم تصحيحي مؤرخ أحدث من الصياغات المتعارضة. نجح فتحهما وفحص بنيتهما بعد التعديل. تعذر التصيير البصري لأن LibreOffice `soffice.exe` غير مثبت؛ لا يُدعى فحص بصري لم يحدث.

الحزمة النهائية: `dist/awqati-0.5.1.4.nvda-addon`، الحجم 12,363,911 بايت، وSHA-256: `149f3d36cc2c2f8ff3cee5f2236e9b82e6ac70ccaab6d1553841fffcde761854`.

المهمة التالية بعد إغلاق هذه المتابعة تبقى 5.2. لا push، ولا تنفيذ وظيفي من 5.3.
## المزامنة والإغلاق

عولج 40 مسار مشروع في `G:\ملفاتي\إضافة أوقاتي`: نُسخ 39 ملفًا، وحذفت النسخة المناظرة المقصودة من `addon/globalPlugins/awqati/nvda_adapter/multipress.py`، ونُسخت الحزمة `dist/awqati-0.5.1.4.nvda-addon`. تحقق SHA-256 من جميع الملفات الموجودة والحزمة، وتحقق غياب الملف المحذوف؛ النتيجة صفر اختلاف. هذا تحقق من نسخة Drive المحلية المتزامنة، لا إثبات لاكتمال الرفع السحابي.