# حالة تنفيذ مشروع Awqati

## الحالة الحالية

- المشروع: Awqati.
- الاسم الظاهر: أوقاتي.
- الإصدار المستهدف: 4.0.
- المرحلة الحالية: المرحلة صفر.
- المهمة 0.1: مكتملة ومغلقة.
- المهمة 0.2: مكتملة ومغلقة.
- المهام المنجزة: 0.1، إنشاء المشروع والبناء ووثائق الإدارة؛ و0.2، تثبيت حدود الطبقات والنماذج المشتركة.
- المهمة التالية المسموح بها: 0.3، توحيد الفحوص والاختبارات الأساسية.
- لم تبدأ المهمة 0.3 ولا أي جزء من المرحلة الأولى.

## مخرجات المهمة 0.2

- ثُبتت حدود الطبقات تحت حزمة `addon/globalPlugins/awqati/`: `domain` و`application` و`infrastructure` و`nvda_adapter`.
- يحتوي Domain على ثلاثة نماذج محايدة فقط:
  - `Instant`: نقطة زمنية تتطلب `datetime` واعيًا بالمنطقة الزمنية.
  - `Location`: معرف واسم محايدان، وإحداثيات صحيحة، ومعرف منطقة زمنية.
  - `DomainEvent`: أساس حدث يحمل وقت وقوعه فقط، دون إنشاء قائمة أحداث مستقبلية.
- الواجهة المشتركة الوحيدة المضافة هي `NowProvider` في Application لأنها مطلوبة صراحة لحقن الزمن.
- يحقق `SystemNowProvider` العقد عند حد Infrastructure ويقرأ ساعة النظام بتوقيت UTC.
- يوجد `EventClock` الحتمي القابل للتغيير داخل `tests/support/` فقط، ولا تدخل شفرة الاختبار حزمة الإنتاج.
- بقي GlobalPlugin بلا منطق منتج. يستخدم مدخل الحزمة `__getattr__` لتحميل فئة الربط من `nvda_adapter` فقط عندما يطلبها NVDA.
- لا توجد Interfaces استباقية، ولا Repository أو Scheduler أو Dispatcher أو إعدادات أو بيانات أو وظائف مستخدم.

## القرار المعماري التفصيلي

- وضعت الطبقات داخل حزمة `globalPlugins/awqati` لأن NVDA يضيف مجلد `globalPlugins` الخاص بالإضافة إلى مسار الحزمة، ولا يضيف جذر الإضافة كحزمة Python عليا.
- المدخل `globalPlugins/awqati/__init__.py` كسول وصغير: استيراد `awqati.domain` أو `awqati.application` من CPython مستقل لا يستورد `globalPluginHandler` أو wxPython، بينما وصول NVDA إلى `GlobalPlugin` يحمّل `nvda_adapter.plugin`.
- تستخدم الوحدات الداخلية استيرادات نسبية، لذلك تعمل الحزمة بالاسم `awqati` في Python المستقل وبالاسم `globalPlugins.awqati` داخل NVDA دون ازدواج للشفرة.

## الاختبارات الآلية المنفذة

- مفسر الفحص الفعلي: Python 3.12.14 المضمّن في بيئة Codex؛ استُخدم مساره المطلق لأن alias النظام `python.exe` يشير إلى WindowsApps ويخرج بالرمز 1 دون تشغيل مفسر.
- `<python.exe> -m compileall -q addon tests buildVars.py tools/run_scons.py`: ناجح، بلا أخطاء.
- `<python.exe> -m unittest discover -s tests -v`: ناجح؛ 15 اختبارًا، 15 ناجحًا، 0 فاشل، 0 خطأ، 0 متخطى.
- اختبارات 0.1 الخمسة في `test_scaffold.py`: ما زالت ناجحة.
- اختبار Domain البنيوي: ناجح؛ يمنع NVDA وwxPython وواجهات المنصة وI/O والطبقات الخارجية.
- اختبار اتجاه الاعتماد: ناجح؛ Application لا تعتمد على Infrastructure أو NVDA Adapter، وDomain لا تعتمد على أي طبقة خارجية.
- فحص import graph: ناجح؛ لا توجد دورة استيراد بين وحدات المشروع.
- اختبار Python المستقل: ناجح في subprocess مع `-S` و`PYTHONPATH=addon/globalPlugins;tests`؛ استورد Domain وApplication وأنشأ النماذج واستخدم `NowProvider` و`EventClock` دون stubs أو monkeypatch ودون تحميل `globalPluginHandler` أو `wx`.
- اختبارات النماذج: ناجحة؛ شملت الوقت الواعي، هوية الموقع وإحداثياته، وأساس الحدث.
- اختبارات الزمن: ناجحة؛ `EventClock` حتمي، يقبل تغيير القيمة مباشرة، ولا يستخدم sleep أو ساعة النظام.
- `git diff --check`: ناجح.

## البناء والحزمة

- تنظيف SCons ثم البناء بمفسر Python الفعلي و`PYTHONPATH=.build-deps`: ناجح.
- الحزمة: `dist/awqati-0.0.0.nvda-addon`.
- فحص ZIP: ناجح؛ `testzip()` لم يجد تلفًا.
- الحزمة تضم manifest ومدخل GlobalPlugin وملفات الطبقات والنماذج والعقد المطلوبة.
- الحزمة لا تضم `__pycache__` أو `.pyc` أو `.pyo`.
- لا توجد حزمة `.nvda-addon` في جذر المشروع.

## اختبار القبول العملي داخل NVDA

- إصدار NVDA المختبر: 2026.2 AMD64، ويستخدم Python 3.13.13.
- ثُبتت/حُدثت إضافة «أوقاتي» من الحزمة الناتجة، وتطابقت ملفات الحزمة المثبتة بـSHA-256.
- أُعيد تشغيل NVDA فعليًا، ثم شُغل مرة بسجل debug مؤقت للتحقق من التحميل.
- سجل الاختبار أثبت العثور على `awqati 0.0.0`، وإضافتها إلى مسار `globalPlugins`، ثم اكتمال `NVDA initialized`.
- لا يوجد traceback أو `ImportError` أو `ModuleNotFoundError` متعلق بـAwqati في جولة القبول النهائية.
- أعيد NVDA إلى التشغيل العادي بعد الاختبار.
- ظهرت في السجلات أخطاء تخص إضافات أخرى ومجلد حذف مؤقت لإضافة أخرى؛ لم تُعدل لأنها خارج نطاق Awqati.

## أعطال اكتشفت وأصلحت

- كشف أول اختبار حي أن وضع النواة في `addon/awqati` ينجح في CPython لكنه يفشل داخل NVDA لأن جذر الإضافة ليس على `sys.path`. نُقلت الطبقات إلى حزمة Awqati تحت `globalPlugins`، وجُعل تحميل adapter كسولًا، ثم أعيدت جميع الاختبارات والبناء والتثبيت واختبار NVDA بنجاح.
- أصلحت اختبارات التطوير نفسها بعد أن عد كاشف الدورات الحزمة الأم اعتمادًا ذاتيًا عند استيراد وحدة فرعية، ووُحد مسار استيراد `tests/support`.

## قيم تطويرية مؤقتة وغير محسومة

- إصدار الحزمة `0.0.0` قيمة بناء تطويرية، وليس رقم إصدار نشر.
- `minimumNVDAVersion` مضبوط مؤقتًا على `2026.1.0`، ولم يُثبت باختبار حي على NVDA 2026.1.
- `lastTestedNVDAVersion` هو `2026.2.0` استنادًا إلى اختبار التثبيت والتحميل الفعلي على NVDA 2026.2 AMD64.
- المؤلف وبيانات الاتصال غير محسومة؛ قيمة المؤلف في manifest موسومة بأنها development placeholder.
- الترخيص و`licenseURL` غير محسومين.
- `sourceURL` وURL الدعم غير محددين لأن مستودع نشر حقيقيًا لم يُعتمد.
- قناة الحزمة `dev` إلى أن تكتمل بوابة الإصدار.

## المشكلات أو العوائق المعروفة

- لا توجد عوائق تمنع إغلاق المهمة 0.2.
- alias النظام للأمر `python` غير صالح حاليًا؛ هذا عيب في بيئة الجهاز لا في المشروع، وقد استُخدم مفسر Python صالح بمساره المطلق.
- عناصر النشر المؤقتة أعلاه تُحسم في مراحلها اللاحقة ولا تمنع قبول المهمة.

## مزامنة نسخة المراجعة

- حُدثت 16 نسخة من ملفات المصدر والاختبارات و`STATUS.md` وحزمة التثبيت في `G:\ملفاتي\إضافة أوقاتي`.
- تطابقت الملفات الستة عشر مع المشروع المحلي بواسطة SHA-256؛ لا توجد اختلافات.
- بقي في نسخة المراجعة تلوث قديم سابق للمهمة داخل `.git` و`.build-deps` و`build` و`__pycache__` وملف `.sconsign.dblite`. لم يُحذف لأن المطلوب تجنب حذف مخاطِر في النسخة المتزامنة، ولم يُنسخ أو يُحدّث ضمن 0.2.
